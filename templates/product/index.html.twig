{% extends 'base.html.twig' %}

{% block title %}Mes produits{% endblock %}

{% block content %}
    <h1>Mes produits</h1>

    <div class="row product-container">
        {% for product in products %}
            <div class="col-md-4">
                <div class="product-item text-center">
                    <a href="{{ path('product', { slug : product.slug }) }}"><img src="/uploads/{{ product.illustration }}" alt="{{ product.illustration }}" class="img-fluid"></a>
                    <h5>{{ product.name }}</h5>
                    <span class="product-subtitle">{{ product.subtitle }}</span>
                    <span class="product-price">{{ product.price/100 }} XPF</span>
                </div>
            </div>
        {% endfor %}
    </div>
{% endblock %}
